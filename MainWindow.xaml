<Window x:Class="Snack.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Snack.ViewModels"
        xmlns:conv="clr-namespace:Snack.Converters"
        mc:Ignorable="d"
        Title="Snake Game - WPF"
        Height="600" Width="600"
        Background="#222"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <conv:CellPositionConverter x:Key="CellPositionConverter" />
    </Window.Resources>

    <Window.DataContext>
        <vm:GameViewModel />
    </Window.DataContext>

    <DockPanel Margin="10">
        <!-- TOP BAR -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0 0 0 10">
            <TextBlock Text="Score: "
                       Foreground="White" FontSize="20"
                       VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Score}"
                       Foreground="Lime" FontSize="20"
                       Margin="5 0"/>

            <TextBlock Text=" | "
                       Foreground="White" FontSize="20"
                       Margin="10 0"/>

            <TextBlock Text="{Binding StatusText}"
                       Foreground="Yellow" FontSize="20"
                       VerticalAlignment="Center"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Start"
                        Margin="20 0 5 0"
                        Padding="10,5"
                        Command="{Binding StartCommand}"/>
                <Button Content="Pause"
                        Margin="0 0 5 0"
                        Padding="10,5"
                        Command="{Binding PauseCommand}"/>
                <Button Content="Restart"
                        Margin="0"
                        Padding="10,5"
                        Command="{Binding RestartCommand}"/>
            </StackPanel>
        </StackPanel>

        <!-- GAME AREA -->
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
            <Canvas x:Name="GameCanvas"
                    Background="Black"
                    Width="400" Height="400">

                <!-- SNAKE -->
                <ItemsControl ItemsSource="{Binding SnakeParts}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Canvas.Left">
                                <Setter.Value>
                                    <MultiBinding Converter="{StaticResource CellPositionConverter}">
                                        <Binding Path="Column"/>
                                        <Binding Path="DataContext.CellSize" RelativeSource="{RelativeSource AncestorType=Window}"/>
                                    </MultiBinding>
                                </Setter.Value>
                            </Setter>

                            <Setter Property="Canvas.Top">
                                <Setter.Value>
                                    <MultiBinding Converter="{StaticResource CellPositionConverter}">
                                        <Binding Path="Row"/>
                                        <Binding Path="DataContext.CellSize" RelativeSource="{RelativeSource AncestorType=Window}"/>
                                    </MultiBinding>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ItemsControl.ItemContainerStyle>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Rectangle Width="{Binding DataContext.CellSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                       Height="{Binding DataContext.CellSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                       Fill="LimeGreen" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- FOOD -->
                <Rectangle Fill="Red"
                           Width="{Binding DataContext.CellSize, RelativeSource={RelativeSource AncestorType=Window}}"
                           Height="{Binding DataContext.CellSize, RelativeSource={RelativeSource AncestorType=Window}}">
                    <Rectangle.RenderTransform>
                        <TranslateTransform>
                            <TranslateTransform.X>
                                <MultiBinding Converter="{StaticResource CellPositionConverter}">
                                    <Binding Path="Food.Column" />
                                    <Binding Path="DataContext.CellSize" RelativeSource="{RelativeSource AncestorType=Window}"/>
                                </MultiBinding>
                            </TranslateTransform.X>

                            <TranslateTransform.Y>
                                <MultiBinding Converter="{StaticResource CellPositionConverter}">
                                    <Binding Path="Food.Row" />
                                    <Binding Path="DataContext.CellSize" RelativeSource="{RelativeSource AncestorType=Window}"/>
                                </MultiBinding>
                            </TranslateTransform.Y>
                        </TranslateTransform>
                    </Rectangle.RenderTransform>
                </Rectangle>

            </Canvas>
        </Grid>
    </DockPanel>
</Window>